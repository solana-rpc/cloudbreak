# Third-party code. See README.md for provenance and the list of local changes.
[package]
name = "clmm_liquidity_check"
version = "0.1.0"
edition = "2021"

[dependencies]
ureq = { version = "2", default-features = true, features = ["brotli"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
bs58 = "0.5"
